/// Layout constants for expense-related UI components.
///
/// Used by both the controller (for scroll offset calculations)
/// and UI widgets for consistent sizing.
abstract final class ExpenseLayout {
  /// Height of category section headers in the category tab.
  static const double categoryHeaderHeight = 48.0;

  /// Height of individual expense rows.
  static const double expenseRowHeight = 72.0;
}
